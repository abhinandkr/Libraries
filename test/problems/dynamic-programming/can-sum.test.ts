import { canSum } from '../../../src/problems/dynamic-programming/can-sum'

describe('can-sum', function() {
  it('should test can-sum', function() {
    expect(canSum(300, [7, 14])).toEqual(true)
  })
})
